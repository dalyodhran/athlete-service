apply plugin: "war"

bootWar {
    mainClass = "com.coursecrafter.athleteservice.AthleteServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
